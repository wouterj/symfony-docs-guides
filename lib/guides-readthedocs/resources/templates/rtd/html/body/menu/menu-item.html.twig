<li class="toctree-l{{ node.level }}{{ node.isCurrent ? ' current' }}">
    <a href="{{ url }}">
        {{ renderNode(node.value.value) }}
    </a>
    {%- set entries = node.children|default([])|merge(node.sections|default([])) -%}
    {% if entries|length %}
        {% include "body/menu/menu-level.html.twig" with {
            entries: entries
        } %}
    {% endif %}
</li>
